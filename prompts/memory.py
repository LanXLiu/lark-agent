"""对话记忆相关 prompt：摘要压缩 + flush 抽关键事实（拼接逻辑仍在 service/memory.py）。"""

# flush：摘要压缩前，从「将被压缩的老轮次」抽取确定的关键事实，防摘要糊掉硬信息。
FLUSH_FACTS_SYSTEM = (
    "你是事实提取助手。请从对话中抽取后续追问可能用到的确定事实："
    "具体数字、日期、名称、结论、用户明确表达的偏好或决定。"
    "只提取对话里真实出现的信息，不要推测、改写或泛泛总结。"
    "每条一行，用「- 」开头；没有可提取事实时输出空。只输出事实列表。"
)

FLUSH_FACTS_USER_TMPL = "对话：\n{convo}\n\n关键事实："

# 摘要：把「已有摘要 + flush 关键事实 + 新溢出老轮次」增量压缩成一份更新后的完整摘要。
# {approx_chars} 是给 LLM 的目标字数提示；实际仍由 service/memory.py 按 token 硬截断。
SUMMARY_SYSTEM_TMPL = (
    "你是对话摘要助手。请把对话历史压缩成简洁中文要点，"
    "保留用户关心的主题、已确认的关键事实、决定和结论，去掉寒暄与重复。"
    "如果提供了「必须保留的关键事实」，要逐条并入摘要，不得遗漏。"
    "如果已有摘要，请把新增内容融合进去，输出一份更新后的完整摘要。"
    "目标长度约 {approx_chars} 字以内；这是软约束，准确性和关键事实优先。只输出摘要正文。"
)

SUMMARY_USER_TMPL = "{prev}{facts_block}新增对话：\n{convo}\n\n更新后的摘要："

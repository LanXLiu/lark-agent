"""答案生成 prompt 的固定文案（片段拼接逻辑在 app/assistant/qa_service.py）。"""

QA_USER_PROMPT_HEADER = "请根据以下知识库片段回答用户问题。\n\n"

QA_ANSWER_REQUIREMENTS = (
    "\n\n回答要求：\n"
    "1. 以知识库片段为唯一依据，不要编造片段中没有的信息。\n"
    "2. 多个片段同等重要；若它们属于同一主题的不同层级、步骤或子项，"
    "请合并整理并尽量覆盖完整，不要只挑一段回答。\n"
    "3. 若片段只能回答部分问题，请明确可回答的范围；不要把不确定内容说成确定事实。\n"
    "4. 表达要条理清晰、简洁，不要在正文里输出过长原文引用。"
)
